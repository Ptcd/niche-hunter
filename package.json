{
  "name": "niche-hunter",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered local lead-generation opportunity finder",
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@niche-hunter/ai": "*",
    "inngest": "^3.46.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run db:generate --workspace=packages/db && npm run build --workspace=packages/db && npm run build --workspace=packages/ai && npm run build --workspace=packages/core && npm run build --workspace=packages/crawler && npm run build --workspace=packages/deterministic-generator && npm run build --workspace=apps/web",
    "build:web": "npm run db:generate --workspace=packages/db && npm run build --workspace=packages/db && npm run build --workspace=packages/ai && npm run build --workspace=packages/core && npm run build --workspace=packages/crawler && npm run build --workspace=packages/deterministic-generator && npm run build --workspace=apps/web",
    "db:generate": "npm run db:generate --workspace=packages/db",
    "db:migrate": "npm run db:migrate --workspace=packages/db",
    "db:studio": "npm run db:studio --workspace=packages/db"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
